[project]
name = "span-labeling"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "accelerate>=1.10.1",
    "ipykernel>=6.30.1",
    "ollama>=0.6.0",
    "openai>=2.1.0",
    "syntok>=1.4.4",
    "torch>=2.8.0",
    "wtpsplit==2.1.6",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
